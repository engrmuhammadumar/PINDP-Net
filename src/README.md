# Genuine source code required

Copy the actual final implementation used for the manuscript: feature
extraction, fold-fitted preprocessing, PINDP-Net and baseline definitions,
nested model selection, ensemble training, inference, uncertainty, metrics,
threshold crossing, and plotting. Do not publish invented placeholder
implementations.
