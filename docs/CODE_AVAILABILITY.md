# Code Availability template

Use only after every claimed artifact is actually available:

The complete PINDP-Net implementation and reproducibility package are publicly
available at [REPOSITORY URL]. An immutable release is archived at
[ARCHIVE DOI] under the [LICENSE] license. The release contains source code,
configurations, environment specifications, seeds, cutter-level splits,
preprocessing information, selected features, training histories, permitted
checkpoints, per-cutter predictions, direct baseline outputs, exact plotted
values, evaluation scripts, diagnostics, latency records, and reproduction
commands.

Remove every bracketed placeholder before submission.
